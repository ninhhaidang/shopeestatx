{
  "name": "shopeestatx",
  "version": "2.6.0",
  "private": true,
  "type": "module",
  "description": "Chrome Extension thống kê chi tiêu Shopee",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^28.1.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "chart.js": "^4.4.7",
    "xlsx": "^0.18.5"
  }
}
